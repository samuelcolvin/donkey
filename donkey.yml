
foo:
  parallel: true
  run:
  - echo first
  - sleep 1
  - echo second

bar:
#  mode: script
  run:
  - "echo bar;sleep 1; printf 'this and that\n'"

py:
  interpreter: python
  script: true
  run:
  - "a = 1 + 2"
  - "print('a = {}'.format(a))"

.default: foo
